package academy.everyonecodes.java.evaluation2.optionals.Enums;

import java.util.ArrayList;
import java.util.List;

public class MostExpensiveDishesFinder {

    public List<Dish> find(List<Dish> menu) {
        List<Dish> result = new ArrayList<>();
        //menu.stream()
        //        .sorted(Comparator.comparing(Dish::getPrice)
        //       .thenComparing(Dish::getDishCategory))
        //        .collect(Collectors.toList());

        for (DishCategory.DESSERT :DishCategory.values()){

        }
        for (DishCategory.MAINDISH :DishCategory.values()){

        }
        for (DishCategory.DESSERT :DishCategory.values()){

        }
        return result;
    }
